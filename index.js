function test() {
  const test = {
    test: 3,
    yes: {
      no: [1, 2, 3],
    },
  };

  const test = () => {
    if (test !== 1) {
      return 1;
    }
  };

  const f = new BigInt(2);

  return f * 2n;
}
